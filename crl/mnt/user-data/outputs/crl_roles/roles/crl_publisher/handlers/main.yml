---
# roles/crl_publisher/handlers/main.yml

- name: restart apache
  ansible.builtin.systemd:
    name: "{{ crl_publisher_apache_service }}"
    state: restarted

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
