---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart crl-fetch timer
  ansible.builtin.systemd:
    name: crl-fetch.timer
    state: restarted
    enabled: true
  when: not ansible_check_mode
